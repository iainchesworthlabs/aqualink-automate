# constexpr opportunities — aqualink-automate C++23 codebase

_Branch `refactor/looking-for-constexpr` · generated by a multi-agent find→verify workflow over all 1,025 `.cpp/.h/.hpp` files in `src/`._

Each candidate was proposed by a scanning agent, then **independently re-opened and verified** against the real code by a second agent. Only CONFIRMED / DOWNGRADED items appear below; false positives were dropped.

## Summary

- **147 confirmed** opportunities · **3 rejected** as false positives
- By confidence: **98 high**, 38 medium, 11 low
- By refactor effort: **45 none** (add a keyword), **79 minor**, 21 moderate, 2 significant

### By category

| Category | Count |
|---|---:|
| free-function -> constexpr | 45 |
| member-function -> constexpr | 34 |
| enum<->string map -> constexpr | 26 |
| local/global const -> constexpr/constinit | 14 |
| checksum / bit / framing math -> constexpr | 12 |
| constructor -> constexpr | 7 |
| unit / temperature conversion -> constexpr | 5 |
| lookup-table / array -> constexpr | 4 |

### By subsystem

| Subsystem | Count |
|---|---:|
| `src/jandy/devices` | 28 |
| `src/core/kernel` | 17 |
| `src/core/scheduling` | 13 |
| `src/core/http` | 12 |
| `src/jandy/messages` | 10 |
| `src/core/auth` | 9 |
| `src/core/utility` | 9 |
| `src/jandy/utility` | 6 |
| `src/core/interfaces` | 5 |
| `src/jandy/startup` | 5 |
| `src/core/logging` | 5 |
| `src/jandy/factories` | 4 |
| `src/jandy/navigation` | 4 |
| `src/core/alerting` | 3 |
| `src/jandy/errors` | 3 |
| `src/jandy/auxillaries` | 2 |
| `src/pentair/messages` | 2 |
| `src/core/platform` | 2 |
| `src/core/errors` | 2 |
| `src/core/devices` | 1 |
| `src/core/preferences` | 1 |
| `src/pentair/utility` | 1 |
| `src/core/mqtt` | 1 |
| `src/core/serial` | 1 |
| `src/jandy/options` | 1 |

## Tier 1 — Quick wins (42): add `constexpr`, zero body change, high confidence

These compile as constexpr today with only the keyword added — pure functions/predicates over literal (enum / string_view / arithmetic) types.

| # | File | Symbol | Category | Why it qualifies | Benefit |
|--:|---|---|---|---|---|
| 1 | `src/core/alerting/alert_monitor.cpp`:38 | `IsChlorinatorFault` | free-function -> constexpr | Pure predicate: return depends only on the enum argument (Kernel::ChlorinatorHealth), a literal type; two equality comparisons, no I/O, no allocation, no virtual dispatch, no throwing, no static state. | Compile-time-evaluable fault classification; enables constexpr callers and lets the compiler fold the comparison; also documents the function as side-effect-free. |
| 2 | `src/core/alerting/alert_monitor.cpp`:46 | `ChlorinatorFaultLabel` | enum<->string map -> constexpr | Pure switch mapping an enum value to a static string literal returned as std::string_view (literal type). No allocation, no I/O, no mutable state; result depends only on the argument. | Compile-time enum->label lookup with no runtime branch cost when the argument is constant; string_view over string literals is fully constexpr in C++23. |
| 3 | `src/core/alerting/alert_monitor.cpp`:61 | `ChlorinatorWarningLabel` | enum<->string map -> constexpr | Pure switch mapping an enum to std::optional<std::string_view> (a literal type in C++23) or std::nullopt; depends only on the argument, no allocation/I/O/mutable state. | Compile-time warning-label lookup; std::optional<string_view> of string literals is constexpr in C++23, so constant callers fold the whole switch. |
| 4 | `src/core/auth/entitlement.cpp`:11 | `IsValidActionChar` | free-function -> constexpr | Pure per-character predicate over literal char ranges; no side effects, no state, no I/O. | Compile-time evaluable, inlinable; enables constexpr use in the validator it feeds. |
| 5 | `src/core/auth/group_store.cpp`:12 | `IsBuiltInName` | free-function -> constexpr | Pure predicate comparing a string_view against three constexpr BuiltInGroups string_views; no side effects, no I/O. | Compile-time evaluable and inlinable; matches the constexpr nature of the BuiltInGroups constants it compares against. |
| 6 | `src/core/http/webroute_command_helpers.h`:21 | `StatusForCommandResult` | free-function -> constexpr | Pure total function: a single switch mapping the ICommandDispatcher::CommandResult enum to an HTTP::Status enum. Both parameter and result are literal (scoped-enum) types; no I/O, no allocation, no dispatch, no side effects, result depends only on the argument. | Enables compile-time evaluation of the status mapping and reliable inlining; lets callers use the result in constant expressions. It is already declared inline in a header so no ABI/link change. |
| 7 | `src/core/http/webroute_equipment_chlorinator.cpp`:24 | `StatusFor` | free-function -> constexpr | Pure total mapping from CommandResult enum to HTTP::Status enum via a plain switch; no I/O, no allocation, no side effects, both input and output are literal enum types. Result depends only on the argument. | Compile-time evaluable enum-to-enum mapping; enables inlining/const-folding at call sites and documents the function as pure. Trivial one-word change (add constexpr). |
| 8 | `src/core/interfaces/iwebroute.h`:24 | `AccessRequirement::IsSpecified` | member-function -> constexpr | Pure predicate over the struct's own std::string_view member (Action.empty()); no I/O, allocation, virtual dispatch, or side effects, and string_view::empty() is itself constexpr. AccessRequirement is a literal aggregate. | Compile-time evaluable: a route's fixed AccessRequirement (built from string-literal Action/ResourceKind) can be checked at compile time; enables constexpr use in the full-surface enforcement checks and inlining. |
| 9 | `src/core/kernel/ph.cpp`:15 | `pH::operator()` | member-function -> constexpr | Returns the float member by value; pure, no side effects, literal type. | Allows reading pH in constant expressions and full inlining. |
| 10 | `src/core/scheduling/controller_schedule.cpp`:32 | `ControllerScheduleStatusToString` | enum<->string map -> constexpr | Pure switch mapping a literal enum to string_view literals; no allocation or side effects. | Compile-time enum-to-string mapping; foldable and constexpr-usable. |
| 11 | `src/core/scheduling/promotion_constraints.cpp`:14 | `TimeInRange` | free-function -> constexpr | Pure predicate over two ints with only integer comparisons; no side effects. | Compile-time range check; usable in constexpr contexts and inlinable as a constant guard. |
| 12 | `src/core/scheduling/promotion_constraints.cpp`:85 | `IsControllerRepresentableAction` | free-function -> constexpr | Pure: compares an ActionType enum value against two literals; no state or side effects. | Compile-time action gate; foldable predicate for constexpr callers. |
| 13 | `src/core/scheduling/promotion_constraints.cpp`:20 | `ControllerDaySelectionToString` | enum<->string map -> constexpr | Pure switch mapping a literal enum to string_view literals; no allocation (string_view over static data), no side effects. | Compile-time enum-to-string; enables constexpr use and constant folding. |
| 14 | `src/core/scheduling/promotion_constraints.cpp`:49 | `PromotionBlockerToString` | enum<->string map -> constexpr | Pure switch mapping a literal enum to string_view literals; no side effects. | Compile-time enum-to-string mapping usable in constexpr contexts. |
| 15 | `src/core/scheduling/schedule.cpp`:29 | `IsButtonAction` | free-function -> constexpr | Pure: three enum-literal comparisons on an ActionType; no side effects. | Compile-time button-action predicate; foldable in constexpr callers. |
| 16 | `src/core/scheduling/schedule.cpp`:35 | `IsSetpointAction` | free-function -> constexpr | Pure: two enum-literal comparisons on an ActionType; no side effects. | Compile-time setpoint-action predicate; foldable. |
| 17 | `src/core/scheduling/schedule.cpp`:116 | `ActionTypeToString` | enum<->string map -> constexpr | Pure linear scan over the already-constexpr ACTION_NAMES std::array comparing enum values and returning string_view over static data; no allocation or side effects. | Compile-time enum-to-string over the constexpr table; constexpr-usable and foldable. |
| 18 | `src/core/scheduling/schedule.cpp`:125 | `ActionTypeFromString` | enum<->string map -> constexpr | Pure linear scan over the constexpr ACTION_NAMES array comparing string_views; returns std::optional<ActionType> (literal type); no allocation or side effects. | Compile-time string-to-enum parsing usable in constexpr contexts (e.g. compile-time validation of known tokens). |
| 19 | `src/core/scheduling/schedule_authorization.cpp`:19 | `ActionEntitlement` | enum<->string map -> constexpr | Pure switch mapping an ActionType to inline constexpr std::string_view vocabulary constants; no allocation or side effects. | Compile-time action-to-entitlement mapping; foldable and constexpr-usable. |
| 20 | `src/core/scheduling/schedule_authorization.cpp`:40 | `IsButtonAction` | free-function -> constexpr | Pure: three enum-literal comparisons on an ActionType; no side effects (duplicate of the schedule.cpp predicate, also constexpr-able). | Compile-time button-action predicate; foldable. |
| 21 | `src/core/utility/json_serialization_helpers.h`:16 | `NanosToMicros` | unit / temperature conversion -> constexpr | Pure literal-type conversion: reads only the nanoseconds argument's .count() (constexpr) and divides by a literal; no side effects. | Compile-time evaluable divide; enables constexpr use in serialization helpers. |
| 22 | `src/core/utility/signalling_stats_counter.h`:128 | `AnyEnumHash::Mix` | checksum / bit / framing math -> constexpr | Static pure bit-mixing over two std::size_t values (xor/add/shifts); no state, allocation, or I/O. | Compile-time constant-foldable hash combine; documents purity, no ABI/behaviour change. |
| 23 | `src/jandy/auxillaries/jandy_powercenter_mapping.h`:17 | `PowerCenterForAuxId` | member-function -> constexpr | Pure function of one enum argument (JandyAuxillaryIds, a literal type); body is only a static_cast to uint8_t plus integer range comparisons returning std::optional<Kernel::PowerCenterIds> (literal type). No I/O, allocation, virtual dispatch, logging, or mutable state. | Compile-time evaluation of the aux-id -> power-centre mapping; enables use in constexpr contexts / static_asserts and lets the compiler fold the range checks at call sites where the id is known. |
| 24 | `src/jandy/devices/aquarite_device.cpp`:27 | `ConvertToChlorinatorHealthStatus` | enum<->string map -> constexpr | Pure total switch mapping Messages::AquariteStatuses -> Kernel::ChlorinatorHealth; result depends only on the enum argument, no I/O, allocation, virtual dispatch, or mutable state. | constexpr-evaluable enum translation; compiler can fold the mapping at call sites and it becomes usable in constexpr contexts (e.g. compile-time tests of the health mapping). |
| 25 | `src/jandy/devices/aquarite_device.cpp`:50 | `IsConcerningHealth` | member-function -> constexpr | Pure predicate over a single enum value (health != Ok && != TurningOff); no side effects, no state. | compile-time-evaluable boolean; enables constexpr use and inlining of the sticky/raise decision. |
| 26 | `src/jandy/devices/aquarite_device.h`:44 | `AquariteTypeWithTimeComparator::operator()` | member-function -> constexpr | Pure equality comparison of two struct.value fields (uint8_t / uint16_t); no side effects, literal inputs. | constexpr comparator; also should be const-qualified. Usable at compile time and inlinable in the debouncer. |
| 27 | `src/jandy/devices/chlorinator_setpoint_refresh.h`:37 | `ChlorinatorSetpointRefresh::Configure / Disable / MarkStartupComplete / NotifyScrapeStarted / NotifyScrapeFinished` | member-function -> constexpr | Trivial mutators over literal-type members (bool/std::chrono::seconds/time_point); no side effects beyond own state, no I/O. | makes the object usable in constexpr contexts alongside Evaluate; enables compile-time construction/driving of the policy for tests. |
| 28 | `src/jandy/devices/chlorinator_setpoint_refresh.h`:103 | `ChlorinatorSetpointRefresh::IsEnabled / Interval / StartupComplete / RecoveryPending` | member-function -> constexpr | Pure const accessors returning literal-type members; no side effects. | constexpr accessors complete the literal-type contract of the class. |
| 29 | `src/jandy/devices/command_dispatcher.cpp`:28 | `ToActuationAction` | enum<->string map -> constexpr | Pure total switch mapping ICommandDispatcher::DeviceAction -> Capabilities::ActuationAction; depends only on the argument, no side effects. | constexpr enum translation, foldable at every dispatch call site. |
| 30 | `src/jandy/devices/command_dispatcher.cpp`:38 | `ActionVerb` | enum<->string map -> constexpr | Pure switch mapping DeviceAction -> string literal; std::string_view over string literals is a literal type, no allocation. | constexpr string_view lookup; the verb table can be resolved at compile time. |
| 31 | `src/jandy/devices/iaq/iaq_schedule_writer.cpp`:75 | `DayCommandFor` | checksum / bit / framing math -> constexpr | Pure bit-twiddling over a uint8_t: masks with 0x7f then dispatches on equality / std::popcount / std::countr_zero (all constexpr in C++23) to a command byte. No side effects, no state, literal in/out. Currently in an anonymous namespace so a constexpr version stays TU-local. | Compile-time folding of the day-bitmask -> touch-cell command mapping; enables constant-expression use and lets the compiler precompute results for literal masks. |
| 32 | `src/jandy/devices/lights_device.cpp`:26 | `ConvertToAuxillaryStatus` | enum<->string map -> constexpr | Anonymous-namespace helper that is a pure enum-to-enum switch (Messages::LightStates -> Kernel::AuxillaryStatuses) with no I/O, allocation, or state; result depends only on the argument. | Compile-time-evaluable mapping and guaranteed inlining of the state translation; lets callers fold known-constant states at compile time. |
| 33 | `src/jandy/devices/onetouch/onetouch_schedule_parser.cpp`:13 | `Trim` | free-function -> constexpr | Pure string_view trimmer: remove_prefix/remove_suffix over leading/trailing space/tab. All string_view ops are constexpr; no allocation/state/side effects. | Trivially constexpr-able helper reused by ParseDaysRow / ParseProgramDetailLines; enables the days-row and detail-line parses to be evaluated at compile time. |
| 34 | `src/jandy/devices/onetouch/onetouch_scraper.h`:99 | `HINT_COUNT` | local/global const -> constexpr/constinit | static const uint32_t {2} used immediately as the size template argument of std::array<unsigned char, HINT_COUNT> (HintArrayType), so it is already required to be a constant expression; the declaration just isn't spelled constexpr. | Marking it static constexpr states the compile-time-constant intent explicitly and matches the adjacent SETAQUAPURE_*_LINE constexpr members; no behavioural change but removes an ODR-const definition need. |
| 35 | `src/jandy/devices/onetouch_device.h`:57 | `ONETOUCH_TIMEOUT_DURATION / ONETOUCH_PAGE_LINES / ONETOUCH_SCRAPING_STALL_LIMIT / ONETOUCH_SETPOINT_REFRESH_STEP_LIMIT / ONETOUCH_FAULT_RECOVERY_STATUS_FRAMES / SETTEMP_POOL_HEAT_LINE / SETTEMP_SPA_HEAT_LINE / ONETOUCH_CHLORINATOR_STEP` | local/global const -> constexpr/constinit | Class-scope inline static const constants initialised from integer/chrono literals (uint8_t/uint32_t and std::chrono::seconds, whose constructor is constexpr). Pure literal-type compile-time constants with no runtime dependency. | inline static constexpr makes them true constant expressions (usable as array bounds / non-type template args, guaranteed no static-init cost, and eligible as compile-time operands where currently only ODR-const). Matches the file's own SETAQUAPURE_*_LINE already-constexpr style. |
| 36 | `src/jandy/devices/pda_device.h`:29 | `PDA_PAGE_LINES / PDA_CONFIG_INIT_SCRAPER / PDA_TIMEOUT_DURATION` | local/global const -> constexpr/constinit | inline static const class constants from a uint8_t literal, a Scrapeable::ScrapeId literal, and a std::chrono::seconds literal (constexpr ctor). All literal types with constant initialisers. | inline static constexpr makes them compile-time constants (PDA_PAGE_LINES is used as a bound/compare in Slot_PDA_MessageLong); no static-init, usable in constant expressions. |
| 37 | `src/jandy/devices/serial_adapter_device.h`:36 | `SERIALADAPTER_TIMEOUT_DURATION / SERIALADAPTER_INVALID_TEMPERATURE_CUTOFF` | local/global const -> constexpr/constinit | inline static const class constants initialised from a std::chrono::seconds literal and a double literal (-17.0) respectively; both literal types, no runtime init needed. | inline static constexpr yields genuine compile-time constants (the -17.0 cutoff is compared in every Command_SerialAdapter_*Temperature guard), removing any static-init and enabling constant-expression use. |
| 38 | `src/jandy/factories/jandy_auxillary_factory.cpp` | `IsSprinklerDevice` | member-function -> constexpr | Body unconditionally returns the literal false; result depends on nothing, no side effects, no I/O, no allocation. | Compile-time constant fold of the predicate; trivially constexpr with no behavior change. |
| 39 | `src/jandy/messages/iaq/iaq_message_main_status.cpp`:29 | `RawToHeaterStatus` | enum<->string map -> constexpr | Pure total function: a switch mapping a uint8_t wire byte to Kernel::HeaterStatuses (a literal-type uint8_t-backed scoped enum). Depends only on its argument, no I/O, no allocation, no logging, no state, no throw. | Marking it constexpr allows compile-time folding of the byte->enum mapping and statically guarantees the helper stays side-effect-free; the compiler can inline it to a jump table at each call site in DeserializeCurrentStatus. |
| 40 | `src/jandy/startup/jandy_startup_planner.cpp`:65 | `MinRevisionForEmulatedType` | free-function -> constexpr | Pure switch over an enum argument returning literal chars / std::nullopt; std::optional<char> is a literal type; no I/O, allocation, virtual dispatch, or static state. | Compile-time evaluable table lookup; enables the callers RevisionGatesOutEmulation gate to fold at compile time when inputs are constant, and documents purity. |
| 41 | `src/jandy/utility/spa_switch_assignment.cpp`:19 | `IsSep` | free-function -> constexpr | Pure predicate: result depends only on its char argument, comparing against literal separator characters; no locale call, no I/O, no state, no allocation. | Enables compile-time evaluation and inlining of the separator test; marking it constexpr (in the anonymous namespace) lets any future constexpr line-parsing use it. Note: the sibling TrimSep/TrimWhitespace cannot follow because they call the non-constexpr std::isspace/std::isdigit. |
| 42 | `src/pentair/messages/pentair_message_decode_helpers.h`:33 | `DataLengthOf` | free-function -> constexpr | Pure single-index read of the LEN header byte from a span argument; depends only on its argument and the Offset_Length literal constant, with no side effects. | constexpr-usable header accessor; permits compile-time use and guaranteed inlining alongside DataByteAt. |

## Tier 2 — Small refactor (72): minor changes, high/medium confidence

| # | File | Symbol | Category | Effort | Why it qualifies | Benefit |
|--:|---|---|---|---|---|---|
| 1 | `src/core/auth/api_key_store.cpp`:24 | `ToHex` | checksum / bit / framing math -> constexpr | minor | Pure bit-twiddling: maps each byte of a std::span to two hex chars via nibble shifts and a constexpr HEX_DIGITS table; no I/O, no state, no throw. std::string building is constexpr in C++23. | Constexpr hex encoding of fixed byte arrays (test vectors, compile-time constants) and inlining of the hot digest-formatting loop. |
| 2 | `src/core/auth/entitlement.cpp`:18 | `IsValidActionPath` | free-function -> constexpr | minor | Pure string_view scan validating the dotted action grammar; only local counters, no allocation/I/O/throw. Depends solely on its argument and the constexpr-capable IsValidActionChar. | Compile-time validation of literal action strings (static_assert on route vocab); inlining. |
| 3 | `src/core/auth/entitlement_vocabulary.cpp`:8 | `Vocabulary::IsKnownAction` | member-function -> constexpr | minor | Pure lookup: linear-searches the constexpr std::array ALL_ACTIONS for a string_view argument via std::ranges::find; no I/O, no allocation, no state. In C++23 std::ranges::find over std::array<string_view> is constexpr-evaluable. | Compile-time membership checks (e.g. static_assert on route-declared actions) and inlining; the whole vocabulary is already a constexpr table so the query can join it. |
| 4 | `src/core/auth/jwt_key_store.cpp`:24 | `ToHex` | checksum / bit / framing math -> constexpr | minor | Identical pure nibble-to-hex helper over a std::span; no side effects, uses only a local constexpr digit table. Constexpr-legal in C++23. | Compile-time hex encoding and inlining; also a candidate to dedupe both ToHex copies into one shared constexpr helper. |
| 5 | `src/core/auth/jwt_key_store.cpp`:40 | `FromHex` | checksum / bit / framing math -> constexpr | minor | Pure hex-decode: validates even length and each nibble via a local pure lambda, packs into a std::vector<uint8_t>; no I/O, no static state, returns nullopt on bad input. std::vector and the loop are constexpr in C++23. | Compile-time hex parsing (test fixtures, embedded key constants) and inlining of the decode loop. |
| 6 | `src/core/devices/capabilities/restartable.h`:18 | `Restartable::DEFAULT_WATCHDOG_TIMEOUT` | local/global const -> constexpr/constinit | minor | It is a const of literal type std::chrono::seconds initialised from the constant expression std::chrono::seconds(30); std::chrono::duration has a constexpr constructor, so the value is fully compile-time with no side effects. It is currently declared const in the header and defined out-of-line in restartable.cpp, incurring a runtime static-init. | Make it 'static constexpr std::chrono::seconds DEFAULT_WATCHDOG_TIMEOUT{ std::chrono::seconds(30) }' inline in the header and delete the out-of-line definition in restartable.cpp: removes a runtime static-initialisation, gives a true compile-time constant usable in constant expressions, and removes the separate TU definition. |
| 7 | `src/core/http/server/responses/html_escape.h`:15 | `HtmlEscape` | free-function -> constexpr | minor | Pure transform: output depends only on the input string_view; body is a char-by-char switch appending to a std::string with no I/O, allocation-that-escapes, virtual dispatch, logging or static state. In C++23 std::string and its reserve/append are constexpr, so the whole body is a valid constant expression. | Enables compile-time escaping of any literal/constexpr input (e.g. static error-body fragments) and lets the compiler constant-fold + inline; adds no runtime cost to the existing runtime callers (response_400/404/405). |
| 8 | `src/core/http/webroute_equipment_circulation.cpp`:27 | `ParseMode` | enum<->string map -> constexpr | minor | Pure function: maps a std::string_view to std::optional<Kernel::CirculationModes> via literal string_view comparisons; no I/O, allocation, virtual dispatch, or side effects. In C++23 std::string_view equality and std::optional over a scoped enum are all constexpr-usable. | Compile-time-evaluable pure dispatch; enables constant folding of literal callers and documents purity. Marks the function usable in constant expressions. |
| 9 | `src/core/http/webroute_equipment_heater.cpp`:29 | `ParseHeaterBody` | enum<->string map -> constexpr | minor | Pure function: maps a std::string_view body name to std::optional<Kernel::BodyOfWaterIds> via literal comparisons; no I/O/allocation/virtual/logging/state. All operations (string_view compare, optional<enum>) are constexpr in C++23. | Compile-time-evaluable pure mapping; constant folding for literal args and an explicit purity guarantee. |
| 10 | `src/core/http/webroute_equipment_iaq.cpp`:23 | `StatusFor` | enum<->string map -> constexpr | minor | Pure total switch over CommandResult returning an HTTP::Status enum value; depends only on its argument, no side effects. A switch over enums returning an enum is a trivially valid constexpr body in C++23. | Compile-time result-code -> HTTP-status mapping; enables constant folding and makes the mapping usable in constant expressions/static_asserts. |
| 11 | `src/core/http/webroute_preferences.cpp`:34 | `IsPerUserKey` | free-function -> constexpr | minor | Pure predicate: single argument std::string_view, body is std::ranges::find over the already-constexpr std::array<std::string_view,4> PER_USER_KEYS compared against end(). ranges::find is constexpr in C++23; no I/O, allocation, virtual dispatch, logging, throwing, or static mutable state. | Enables inlining and compile-time evaluation of the key-classification; the fixed 4-element string_view table can be searched at compile time when the key is known, and the function participates in constant expressions. |
| 12 | `src/core/http/websocket_event.cpp`:15 | `WebSocket_Event::WS_JSON_TYPE_FIELD / WS_JSON_PAYLOAD_FIELD` | local/global const -> constexpr/constinit | minor | Both are static class members of literal type std::string_view initialised directly from string literals ("type"/"payload") — a constant expression with no runtime dependency, no allocation, no side effects. | Compile-time constants with guaranteed constant initialisation (no static-init-order or runtime construction); enables their use in constexpr contexts. Declare as 'static constexpr std::string_view' inline in the header, removing the out-of-line .cpp definitions. |
| 13 | `src/core/interfaces/imessage.h`:13 | `IMessage<MESSAGE_ID>::IMessage(const MESSAGE_ID)` | constructor -> constexpr | minor | Value-holder ctor that only copies a literal-type MESSAGE_ID (opcode/message-type enum) into m_Id; no I/O, allocation, virtual dispatch, or logging. | Enables compile-time construction of message-id wrappers; folds trivial init at compile time. |
| 14 | `src/core/interfaces/imessage.h`:21 | `IMessage<MESSAGE_ID>::Id` | member-function -> constexpr | minor | Pure accessor returning the literal-type m_Id by value; no side effects, no virtual call. | Allows message id to be read in constant expressions (e.g. static_assert / constexpr tables of ids). |
| 15 | `src/core/kernel/auxillary_devices/chlorinator_status.cpp`:6 | `ConvertToChlorinatorStatus` | free-function -> constexpr | minor | Pure total function: a plain switch mapping one scoped enum (AuxillaryStatuses) to another (ChlorinatorStatuses). Inputs are literal (uint8_t-backed enum) types, no I/O, allocation, throwing, virtual dispatch or static state. | Compile-time evaluable enum mapping; enables call sites (e.g. constant-folded status tables/switches) to fold at compile time and lets the mapping be used in constant expressions. Requires marking both declaration (chlorinator_status.h) and definition constexpr and making the definition inline-visible (move body to header). |
| 16 | `src/core/kernel/auxillary_devices/heater_status.cpp`:6 | `ConvertToHeaterStatus` | free-function -> constexpr | minor | Pure total function: switch mapping AuxillaryStatuses to HeaterStatuses. Literal enum inputs, no side effects, no I/O/allocation/throwing/virtual/static state. | Compile-time evaluable enum conversion; usable in constant expressions and constant-folded at call sites. Needs constexpr on declaration in heater_status.h plus an inline-visible definition. |
| 17 | `src/core/kernel/auxillary_devices/pump_status.cpp`:6 | `ConvertToPumpStatus` | free-function -> constexpr | minor | Pure total function: switch mapping AuxillaryStatuses to PumpStatuses. Literal enum inputs, no side effects, no I/O/allocation/throwing/virtual/static state. | Compile-time evaluable enum conversion; foldable at call sites and usable in constant expressions. Needs constexpr on declaration in pump_status.h plus an inline-visible definition. |
| 18 | `src/core/kernel/flow_rate.h`:11 | `FlowRate::FlowRate(const Units::gallons_per_minute&) / FlowRate(const Units::liters_per_minute&)` | constructor -> constexpr | minor | Both ctors just initialise the single m_FlowRateInLPM member from a boost::units::quantity argument (GPM->LPM uses boost::units' constexpr unit conversion). quantity is a literal type; no allocation, I/O, virtual dispatch, or side effects. | Makes FlowRate a literal-type value class; flow-rate constants (e.g. pump-curve thresholds) can be built at compile time and inlined instead of running the conversion at startup. |
| 19 | `src/core/kernel/flow_rate.h`:15 | `FlowRate::InGPM() / FlowRate::InLPM()` | member-function -> constexpr | minor | Pure const accessors: InLPM returns the member; InGPM constructs a gallons_per_minute from it via boost::units' constexpr LPM->GPM conversion. Depends only on the literal-type member; no side effects. | Enables compile-time unit read-back on constexpr FlowRate values and inlines the (currently out-of-line) conversion at call sites. |
| 20 | `src/core/kernel/ph.cpp`:26 | `pH::operator==(const pH&)` | member-function -> constexpr | minor | Pure comparison using std::fabs (constexpr in C++23) and std::numeric_limits::epsilon (constexpr) over float members; no side effects. | Compile-time equality checks; inlining. |
| 21 | `src/core/logging/logging_severity_filter.cpp`:24 | `MakeDefaultSeverityTable` | lookup-table / array -> constexpr | minor | Builds a std::array<Severity, CHANNEL_COUNT> and fills it with the compile-time constant DEFAULT_SEVERITY. std::array::fill is constexpr; no runtime inputs. It exists solely to initialise a static table. | The default per-channel severity table becomes a compile-time constant (ROM/const-init), removing runtime construction and static-init-order concern for the initial fill. |
| 22 | `src/core/logging/sinks/log_environment.cpp`:103 | `ResolveAutoSinks` | free-function -> constexpr | minor | Pure function of a literal-type LogEnvironment struct plus two bools; body is only branches and member assignments on a literal aggregate SinkSelection. No I/O, allocation, virtual dispatch, or static state. Already marked noexcept and documented 'Pure/noexcept'. | Compile-time evaluation of the sink-selection policy in tests/asserts; enables static_assert coverage of the auto policy and lets the compiler fold call sites with known inputs. |
| 23 | `src/core/platform/linux/get_terminal_column_width_prefix.cpp`:8 | `PLATFORM_LOG_PREFIX` | local/global const -> constexpr/constinit | minor | const std::string_view of literal type initialised directly from a string literal ("LINUX:"); pure, no allocation, no side effects, value known at compile time. | Compile-time constant with zero static-init cost; usable in constant expressions and interned in read-only data. Change to `constexpr std::string_view` (the header declaration would also need to become an inline constexpr / extern-const-compatible form). |
| 24 | `src/core/platform/macos/get_terminal_column_width_prefix.cpp`:8 | `PLATFORM_LOG_PREFIX` | local/global const -> constexpr/constinit | minor | const std::string_view of literal type initialised directly from a string literal ("macOS:"); pure, no allocation, no side effects, value known at compile time. | Compile-time constant with zero static-init cost; usable in constant expressions and interned in read-only data. Change to `constexpr std::string_view`. |
| 25 | `src/core/preferences/user_preferences_store.cpp`:28 | `IsKnownKey` | free-function -> constexpr | minor | Pure predicate over the constexpr std::array<std::string_view,4> PER_USER_KEYS using std::ranges::find; std::string_view comparison and ranges::find are constexpr-evaluable in C++23. No I/O, allocation, throwing, or runtime state. | Enables compile-time membership checks and lets the compiler fold/inline the fixed 4-key lookup; makes the constexpr-ness of the whole known-key vocabulary explicit and callable in constant expressions. |
| 26 | `src/core/scheduling/promotion_constraints.cpp`:34 | `ClassifyDaySelection` | free-function -> constexpr | minor | Pure: single std::uint8_t input, only bit-masking and std::popcount (constexpr since C++20) against constexpr day constants; no I/O, allocation, throw, or state. | Compile-time day-selection classification; enables constexpr callers/tests and lets the classification of literal masks fold at compile time. |
| 27 | `src/core/utility/json_serialization_helpers.h`:62 | `RoundToDecimalPlaces` | unit / temperature conversion -> constexpr | minor | Pure: result depends only on (double value, int places); the *10 loop and std::round are all constexpr-eligible in C++23; no allocation, I/O or state. | Compile-time resolution snapping and lets it be used in constexpr contexts; already noexcept. |
| 28 | `src/core/utility/json_serialization_helpers.h`:80 | `RoundTemperatureForDisplay` | unit / temperature conversion -> constexpr | minor | Thin pure wrapper that forwards to RoundToDecimalPlaces(value,1); no side effects. Becomes constexpr once its callee is constexpr. | Consistent constexpr with the underlying rounder; no behaviour change. |
| 29 | `src/core/utility/temperature_conversion.h`:38 | `CelsiusToWireSetpoint` | unit / temperature conversion -> constexpr | minor | Pure function of (double, TemperatureUnits enum); no I/O, allocation, virtual dispatch or static state. Its only callees std::round and std::clamp are both constexpr in C++23, and the sibling CelsiusToFahrenheit it calls is already constexpr. | Compile-time evaluation of fixed setpoints and enables constexpr callers; already noexcept, just add constexpr. |
| 30 | `src/jandy/devices/chlorinator_setpoint_refresh.h`:76 | `ChlorinatorSetpointRefresh::Evaluate` | member-function -> constexpr | minor | Pure decision logic over literal-type members (bools, std::chrono::seconds, steady_clock::time_point are all literal types); mutates only its own literal-type members, no I/O/clock/alloc/virtual. The class doc explicitly states it is designed to be driven by caller-supplied gating booleans and a clock value for isolated testing. | the whole when-to-scrape state machine becomes constexpr-testable and foldable; compile-time unit tests of the trigger latching become possible. |
| 31 | `src/jandy/devices/iaq/iaq_schedule_parser.cpp`:90 | `ParseDayToken` | enum<->string map -> constexpr | minor | Pure function: a fixed chain of std::string_view equality comparisons against literal tokens returning literal-type std::optional<std::uint8_t>. No I/O, allocation, virtual dispatch, or mutable state. std::string_view comparison is constexpr; the returned DayMask constants are already inline constexpr. | Compile-time evaluation of the day-token->mask map; each known-token result can fold at compile time and the function becomes usable in constant expressions (e.g. static assertions on the mask table). |
| 32 | `src/jandy/devices/jandy_device_id.h`:15 | `JandyDeviceId (ctors, operator(), operator==, operator!=)` | constructor -> constexpr | minor | Literal-type value wrapper over a single uint8_t; the two ctors, the call operator and the two comparators are pure, depend only on the uint8_t member, and do no I/O/allocation/virtual dispatch. | Enables compile-time construction/comparison of device ids (usable in constexpr tables and consteval contexts), and lets the std::hash specialization operate on a constexpr-known value; zero runtime cost for trivial accessors. |
| 33 | `src/jandy/devices/jandy_device_types.cpp`:30 | `JandyDeviceType::operator==, operator!=, operator(), Class, Id` | member-function -> constexpr | minor | Each returns/compares only the literal-type members m_DeviceId (JandyDeviceId) and m_DeviceClass (enum); no I/O, allocation, virtual dispatch, or static-state access. Equals() is excluded (uses dynamic_cast / polymorphism). | Compile-time comparison and field access of device types; supports constexpr use of device-type values once the wrapper types are constexpr. |
| 34 | `src/jandy/devices/onetouch/onetouch_schedule_parser.cpp`:35 | `IEquals` | free-function -> constexpr | minor | Pure case-insensitive ASCII string compare over two string_views. The only thing preventing constexpr is std::toupper (from <cctype>, not constexpr); replacing it with an inline ASCII fold ((c>='a'&&c<='z')?c-32:c) makes the whole function a constant expression. | Makes IEquals constexpr, which in turn unblocks constexpr for Parse12HourTime / ParseTimeRow / ParseProgramIndexRow / ParseDaysRow that all depend on it; also avoids the locale-sensitive std::toupper on a pure ASCII comparison. |
| 35 | `src/jandy/devices/onetouch/onetouch_screen_reader.cpp`:41 | `FirstNumber` | free-function -> constexpr | minor | Pure function of a single std::string_view: scans chars, accumulates the first contiguous decimal digit run into an int, returns std::optional<int>. No allocation, I/O, device/DataHub state, tolower, or throwing; every operation (string_view iteration, integer math, std::optional) is constexpr-valid in C++23. | Compile-time evaluable digit-run decode; enables constexpr use and static_assert-based unit tests. It is also the single decode shared by the row-value read and the AquaPure %/Set-AquaPure scrapes, so one constexpr definition covers several call sites (and lets the duplicate parse_percent lambda in onetouch_pageprocessors.cpp be folded onto it). |
| 36 | `src/jandy/factories/jandy_auxillary_factory.h` | `AQUAPURE / CHLORINATOR / CLEANER / HEAT / HEATER / PUMP / SPILLOVER / SPRINKLER / UNKNOWN` | local/global const -> constexpr/constinit | minor | These are fixed string tokens compared/searched at runtime; they hold constant literal data and never mutate. As inline static const std::string they force runtime dynamic-init (heap alloc per string) and static-init ordering. std::string_view over string literals is a literal type and can be constexpr. | Eliminates 9 runtime std::string constructions/heap allocations and their static-init ordering, and is a prerequisite for making the Is*Device predicates constexpr; boost contains/== accept string_view. |
| 37 | `src/jandy/messages/iaq/iaq_message_page_button.h`:43 | `IAQMessage_PageButton::Index_ButtonIndex / Index_ButtonState / Index_ButtonType / Index_ButtonNameText` | local/global const -> constexpr/constinit | minor | These are class-scope integral constants initialised from integer literals; they are pure compile-time values used as span offsets and in size comparisons in DeserializeContents. No runtime dependency. | static constexpr guarantees compile-time (inline) values, removes any risk of an out-of-line-definition ODR-use, and makes intent explicit that they are true constants. |
| 38 | `src/jandy/messages/iaq/iaq_message_page_message.h`:18 | `IAQMessage_PageMessage::Index_LineId / Index_LineText` | local/global const -> constexpr/constinit | minor | Class-scope integral constants set from integer literals and used only as compile-time span offsets / size bounds in the deserializer; no runtime state involved. | static constexpr makes them guaranteed compile-time inline constants, removing ODR-use fragility. |
| 39 | `src/jandy/messages/iaq/iaq_message_page_start.h`:19 | `IAQMessage_PageStart::Index_PageId` | local/global const -> constexpr/constinit | minor | Integer-literal class constant used only as a compile-time payload offset in DeserializeContents; no runtime dependency. | static constexpr makes it a guaranteed compile-time inline constant, removing ODR-use fragility. |
| 40 | `src/jandy/messages/iaq/iaq_message_table_message.h`:24 | `IAQMessage_TableMessage::Index_LineId / Index_Attribute / Index_LineText` | local/global const -> constexpr/constinit | minor | Integer-literal class constants used purely as compile-time payload offsets in DeserializeContents; no runtime dependency. | static constexpr guarantees compile-time inline values and removes out-of-line-definition ODR-use risk. |
| 41 | `src/jandy/messages/iaq/iaq_message_title_message.h`:18 | `IAQMessage_TitleMessage::Index_TitleText` | local/global const -> constexpr/constinit | minor | Single integer-literal class constant used only as a compile-time span offset / size bound; pure literal value. | static constexpr makes it a guaranteed compile-time inline constant. |
| 42 | `src/jandy/navigation/navigator.cpp`:1167 | `Navigator::IsBlockingPage` | member-function -> constexpr | minor | Pure predicate: returns page == PageId::Service \|\| page == PageId::TimeOut. Enum (literal type) input, no use of 'this', no side effects. Effectively a free static predicate. | Compile-time constant-foldable blocking-page test; can become static constexpr (does not touch member state). Note the header declares it non-static const (navigator.h:159) — making it static constexpr is the cleanest form. |
| 43 | `src/jandy/navigation/visit_policies.cpp`:26 | `FullDiscoveryVisitPolicy::IsLabelPage` | member-function -> constexpr | minor | Pure static function: a single switch over the PageId enum (a literal type) returning bool. No I/O, allocation, virtual dispatch, logging, or state — output depends only on the argument. | Compile-time page classification; enables constexpr callers and lets the compiler fold the switch. Declared static already, so add constexpr in both the header decl (visit_policies.h:41) and the definition. |
| 44 | `src/jandy/startup/jandy_startup_planner.cpp`:84 | `RevisionGatesOutEmulation` | free-function -> constexpr | minor | Pure predicate over a literal RevisionCapabilities struct (all char/bool) plus an enum; only calls the (constexpr-able) MinRevisionForEmulatedType and compares chars/bools. No side effects. | Compile-time gating decision; becomes constexpr once MinRevisionForEmulatedType is constexpr, allowing constant-folding in Plan/SelectControllerEmulation. |
| 45 | `src/jandy/utility/jandy_checksum.h`:18 | `JandyPacket_CalculateChecksum_FromRange` | checksum / bit / framing math -> constexpr | minor | Pure fold over a byte-like range: only local uint32 accumulation, static_cast, and std::to_integer; no I/O, allocation, virtual dispatch, logging, or static mutable state. All operations used (ranges::subrange iteration, casts) are constexpr in C++23. | Enables compile-time checksum computation for fixed test/frame literals and constant-folds callers passing constexpr byte arrays; also lets it be used in constexpr contexts and static_asserts for protocol test vectors. |
| 46 | `src/jandy/utility/jandy_checksum.h`:44 | `JandyPacket_CalculateChecksum` | checksum / bit / framing math -> constexpr | minor | Thin pure wrapper: builds a ranges::subrange from two iterators and delegates to the (constexpr-able) FromRange fold. No side effects; subrange construction is constexpr. | Compile-time checksum over iterator pairs; constant-folds and permits static_assert-based validation of known-good frames. |
| 47 | `src/jandy/utility/jandy_null_handler.cpp`:46 | `JandyPacket_NeedsNullCharHandling` | checksum / bit / framing math -> constexpr | minor | Pure scan of a std::span<const uint8_t> for the 0x10,0x00 DLE-null sequence; only index reads and a bool result, no allocation, I/O, logging, or mutable static state. span indexing/iteration is constexpr in C++23. | Compile-time detection of escape sequences for literal frames; lets the predicate participate in constant expressions and static_asserts, and constant-folds when called on constexpr spans. |
| 48 | `src/jandy/utility/jandy_null_handler.cpp`:58 | `JandyPacket_NullCharHandler_DeserializationToSpan` | checksum / bit / framing math -> constexpr | minor | Pure DLE-null de-stuff copying input span into a caller-provided output span with a bounds clamp; only local bool/index state and span element writes, no allocation, I/O, logging, or static state. span iteration and writes are constexpr in C++23. | Enables compile-time de-stuffing of literal frames and static_assert verification of protocol de-escaping test vectors; constant-folds for constexpr buffers. |
| 49 | `src/pentair/messages/pentair_message_decode_helpers.h`:22 | `DataByteAt` | free-function -> constexpr | minor | Pure function of its two arguments (a std::span<const uint8_t> and a uint8_t index) plus the constexpr offset constants; no I/O, no allocation, no logging, no virtual dispatch, no mutable state. std::span indexing and comparison are all usable in a constant expression in C++23. | Enables compile-time evaluation of DATA-field extraction in constexpr tests/decoders and allows inlining without a runtime call; a truly pure bounds-guarded accessor is marked as such. |
| 50 | `src/pentair/utility/pentair_checksum.h`:51 | `AppendBigEndianChecksum` | free-function -> constexpr | minor | Pure function: appends two derived bytes (checksum>>8, checksum&0xFF) to a std::vector<uint8_t> argument; no I/O, no allocation-that-escapes-badly, no virtual dispatch, no static state. In C++23 std::vector and emplace_back are constexpr-usable, so this whole body is a valid constant expression. | Consistency with the two sibling checksum helpers (already constexpr) and enables compile-time frame construction/testing of Pentair 0xA5 checksum framing; currently inline forces runtime-only use. |
| 51 | `src/core/kernel/hub_events/hub_event.cpp`:12 | `Hub_Event::Type` | member-function -> constexpr | none | Trivial pure getter returning the enum member; no side effects. | Inlining; usable in constant expressions. Note the class has a virtual dtor/ToJSON so it is not a literal type, limiting constexpr use of instances. |
| 52 | `src/jandy/devices/iaq/iaq_schedule_parser.cpp`:13 | `Trim` | free-function -> constexpr | none | Pure string_view trimming via remove_prefix/remove_suffix on leading/trailing space/tab; all operations are constexpr on std::string_view. No allocation or side effects. | Constant-expression usable trim; lets the enclosing constexpr parse helpers fold literal inputs at compile time. |
| 53 | `src/jandy/navigation/menu_model.h`:47 | `MenuEdge::IsSelfLoop` | member-function -> constexpr | none | Pure comparison of two PageId (literal enum) members of a literal-type struct: source == target. No side effects; result depends only on the object's literal state. | Marks MenuEdge as a literal value type and lets edge classification (IsSelfLoop/IsPageTransition) be evaluated at compile time; free to add since it is already an inline one-liner. IsPageTransition (line 48) qualifies identically as it just negates IsSelfLoop. |
| 54 | `src/core/auth/entitlement.h`:34 | `Entitlement::Entitlement` | constructor -> constexpr | minor | Trivial member-init constructor of a literal-type value class (std::string + std::optional<std::string>, both constexpr in C++23); moves inputs into members, no side effects. | Enables constexpr Entitlement values and unlocks constexpr Matches/ToString; small strong-type wrapper becomes usable at compile time. |
| 55 | `src/core/http/server/routing/segment_template.h`:72 | `segment_template::operator==` | free-function -> constexpr | minor | Pure comparison over literal-type members (bool is_literal_, std::string str_ which is a literal type in C++23, enum modifier_); no I/O, allocation-that-escapes, virtual dispatch, or side effects. | Enables compile-time comparison of segment templates and marks the function usable in constant expressions; documents purity. |
| 56 | `src/core/http/server/routing/segment_template.h`:94 | `segment_template::operator<` | free-function -> constexpr | minor | Pure precedence ordering over the same literal-type members (is_literal_ bool, modifier_ enum); no side effects, no runtime-only calls. | constexpr-evaluable ordering predicate; consistency with a constexpr operator==. |
| 57 | `src/core/interfaces/imessage.h`:32 | `IMessage<MESSAGE_ID>::operator==` | member-function -> constexpr | minor | Compares only literal-type members (a type-trait bool and m_Id equality); pure, no dispatch or I/O. | Permits compile-time equality checks of message-id wrappers of literal-type MESSAGE_ID. |
| 58 | `src/core/interfaces/imessage.h`:43 | `IMessage<MESSAGE_ID>::SetId` | member-function -> constexpr | minor | Mutates only the literal-type m_Id from a literal argument; no side effects (constexpr member mutation is legal in C++23). | Rounds out the value class as a literal type usable in constant expressions. |
| 59 | `src/core/kernel/flow_rate.h`:19 | `FlowRate::ConvertToFlowRateInGPM(double) / ConvertToFlowRateInLPM(double)` | member-function -> constexpr | minor | Static factories that multiply a double by the boost::units gpm/lpm unit tag and return a FlowRate. Pure arithmetic over literal types (double * unit-tag is constexpr in boost::units); no I/O, allocation, or state. | Allows constexpr FlowRate literals from a plain double at compile time; requires the FlowRate ctors above to also be constexpr. |
| 60 | `src/core/logging/log_sanitize.h`:16 | `SanitizeForLog` | free-function -> constexpr | minor | Pure function of its string_view argument: replaces control/DEL bytes with '?', printable bytes pass through. In C++23 std::string is a literal type with a constexpr constructor/push_back/reserve, so the whole body is constant-evaluable. No I/O or static state. | Allows compile-time sanitization of literal strings and static_assert-based tests of the control-byte mapping; header-only inline already, so no ODR/link cost. |
| 61 | `src/core/logging/sinks/log_environment.cpp`:61 | `JournalStreamMatches` | checksum / bit / framing math -> constexpr | minor | Pure parse/compare over std::optional<std::string>/std::optional<DevIno> inputs (all literal types). Uses only string_view find/substr and ParseUnsigned; std::from_chars for integrals is constexpr in C++23. No side effects; already noexcept and documented 'Pure and noexcept'. | Compile-time-testable $JOURNAL_STREAM matching; static_assert coverage of the parse edge cases (missing colon, non-numeric fields) without runtime execution. |
| 62 | `src/core/logging/sinks/log_environment.cpp`:16 | `ParseUnsigned` | free-function -> constexpr | minor | Pure string_view -> optional<uint64_t> parse using std::from_chars, which is constexpr for integral types in C++23. No allocation, no state, already noexcept. Prerequisite for making JournalStreamMatches constexpr. | Enables the constexpr chain up to JournalStreamMatches and compile-time validation of the numeric parser. |
| 63 | `src/core/mqtt/mqtt_payload_parsing.h`:24 | `PayloadParsing::SanitiseForLog` | free-function -> constexpr | minor | Pure function: transforms a std::string_view into a std::string using only byte comparisons and appends; no I/O, no logging, no allocation that escapes compile-time (constexpr std::string is available in C++23), no throwing, no static/mutable state, no runtime dependency. | Allows compile-time sanitisation of fixed literals and marks the function's purity explicitly; enables constant-folding when called with a constexpr string_view. Note: constexpr result must not exceed the SSO buffer to be usable in a constant expression, but marking still documents/enforces purity. |
| 64 | `src/core/scheduling/promotion_constraints.cpp`:62 | `CheckControllerCandidate` | member-function -> constexpr | minor | Logic is pure over the literal fields of ControllerSchedule (target emptiness, int time ranges, day classification); only side effect is building a PromotionResult whose std::vector is constexpr-constructible in C++23. | Allows constexpr evaluation of promotability for literal candidates; marginal at runtime since results usually escape to callers, but removes a runtime-only barrier and documents purity. |
| 65 | `src/core/utility/bandwidth_utilisation.h`:120 | `UtilisationOverPeriod::ConvertSerialSpeedToBytesPerSecond` | member-function -> constexpr | minor | Pure arithmetic over the literal-type SerialPortOptions argument (character_size, parity, stop_bits, baud_rate); no member access, no I/O, no allocation. Could also be static since it uses no members. | Compile-time byterate for known port options and clarifies it does not touch object state (also a static-candidate). |
| 66 | `src/jandy/auxillaries/jandy_auxillary_id.cpp`:17 | `BankValue` | free-function -> constexpr | minor | Pure helper: switch on a char bank plus 1-based number bounds check, returning std::optional<int> from simple integer arithmetic (0x01 + (number-1) etc). No I/O, allocation, virtual dispatch, or mutable state; all inputs and result are literal types. | Compile-time-evaluable bank/number -> raw-id resolution; folds constant call sites and documents the mapping as pure. (Only consumed at runtime today via ParseAuxId, so benefit is modest.) |
| 67 | `src/jandy/devices/iaq/iaq_schedule_parser.cpp`:57 | `Parse12HourTime` | unit / temperature conversion -> constexpr | minor | Pure 12h->24h clock conversion over string_view + int out-params: uses only std::string_view::find/substr/comparison and std::from_chars, all of which are constexpr in C++23. No allocation, I/O, or state; range-validates and returns bool. | Enables compile-time parsing/validation of literal time tokens and static assertions on the AM/PM->24h conversion; folds constant inputs. |
| 68 | `src/jandy/devices/onetouch/onetouch_schedule_parser.cpp`:47 | `Parse12HourTime` | free-function -> constexpr | minor | Pure decode of an 'h:mm' + AM/PM token pair into 24-hour (hour,minute) via out-params returning bool. std::string_view::find/substr and std::from_chars (integer overload) are all constexpr in C++23; no allocation/I/O/state. Only blocker is its call to IEquals (below), which uses std::toupper. | Compile-time-evaluable clock parsing and static_assert unit tests; once IEquals is made constexpr the whole 12-hour parse becomes a constant expression. |
| 69 | `src/jandy/devices/onetouch/onetouch_schedule_parser.cpp`:89 | `ParseProgramIndexRow` | checksum / bit / framing math -> constexpr | minor | Pure parse of a 'Pgm N of M' row: Tokenise (constexpr std::vector<string_view> in C++23) + IEquals + std::from_chars integer overload (constexpr). No allocation-that-escapes, I/O, or state; only blocker is IEquals' std::toupper. | Compile-time-evaluable index/count extraction and static_assert tests once IEquals is constexpr. |
| 70 | `src/jandy/devices/onetouch/onetouch_schedule_parser.cpp`:109 | `ParseDaysRow` | enum<->string map -> constexpr | minor | Pure token->DayMask (uint8_t) mapping: Trim then a chain of IEquals compares returning std::optional<uint8_t> of literal DayMask constants. No side effects/state; blocked only by IEquals' std::toupper. | Compile-time days-token decode; being the public days-row parser, a constexpr form also allows static_assert coverage of every token. |
| 71 | `src/jandy/factories/jandy_auxillary_factory.cpp` | `IsChlorinatorDevice / IsCleanerDevice / IsSpilloverDevice` | member-function -> constexpr | minor | Each is a pure equality comparison of the argument against fixed literal tokens with no I/O, allocation-that-escapes, virtual dispatch, logging, or mutable state. Becomes fully constexpr-eligible once the compared tokens are constexpr string_view (see companion finding). | Compile-time evaluable predicates over literal inputs; enables the compiler to fold constant-label calls and removes reliance on runtime std::string comparison. |
| 72 | `src/jandy/factories/jandy_auxillary_factory.cpp` | `JandyAuxillaryStatuses->AuxillaryStatuses mapping (switch in SerialAdapterDevice_CreateDevice)` | enum<->string map -> constexpr | minor | The status switch is a pure total enum-to-enum mapping with no side effects, I/O, allocation, or device state; only the surrounding CreateDevice_Impl call is runtime. Extracting the map to a free constexpr function isolates a compile-time-evaluable pure transform. | A reusable constexpr ConvertToAuxillaryStatus(JandyAuxillaryStatuses) that the compiler can fold and that is unit-testable at compile time; deduplicates the ad-hoc inline switch. |

## Tier 3 — Larger / lower-confidence (33): moderate–significant refactor or low confidence

| # | File | Symbol | Category | Effort | Conf | Why / caveat | Benefit |
|--:|---|---|---|---|---|---|---|
| 1 | `src/core/errors/message_errors.cpp`:11 | `Message_ErrorCategory::Describe` | member-function -> constexpr | moderate | high | Pure total switch over a scoped enum returning string_view literals; no I/O, allocation, virtual dispatch, throwing, or static/runtime state — result depends only on the enum argument. | Enables compile-time evaluation of error descriptions and inlining of the string_view lookup; makes the mapping usable in constant expressions (e.g. static_asserts on message text). Requires moving the definition from the .cpp into the header (or declaring it constexpr in-class) since a constexpr function must be visible at the call site. |
| 2 | `src/core/errors/protocol_errors.cpp`:11 | `Protocol_ErrorCategory::Describe` | member-function -> constexpr | moderate | high | Pure total switch over a scoped enum returning string_view literals; no side effects, no allocation, no virtual dispatch, no runtime/device/config state — output is a function of the enum input only. | Compile-time-evaluable enum->description mapping with guaranteed inlining and no runtime function-call overhead for the human-readable text; usable in constant expressions. Requires relocating the definition into the header so it is visible where evaluated. |
| 3 | `src/jandy/devices/onetouch_device.cpp`:294 | `OneTouchDevice::ConvertNavKeyCommand` | enum<->string map -> constexpr | moderate | high | Pure static enum-to-enum mapping: a switch over Navigation::NavKeyCommand returning OneTouchDevice::KeyCommands, both scoped enums (literal types). No side effects, state, or dispatch; a total switch with a default. | Compile-time key-command translation; the definition must move to the header (currently in the .cpp) to be usable in constant expressions. Marks the mapping as pure and lets it be static_assert-checked for exhaustiveness. |
| 4 | `src/jandy/errors/jandy_errors_auxillary_factory.cpp`:11 | `Factory_ErrorCategory::Describe` | enum<->string map -> constexpr | moderate | high | Pure static function: a switch over a literal-type enum (Factory_ErrorCodes) returning compile-time string_view literals. No I/O, no allocation, no virtual dispatch (called non-virtually as DERIVED::Describe from the CRTP base's message()), no side effects. | Compile-time evaluable enum->description lookup; enables constexpr use and lets the compiler fold/deduplicate the literal string_views. Requires moving the switch body from the .cpp into the header so the definition is visible at constexpr call sites. |
| 5 | `src/jandy/errors/jandy_errors_scrapeable.cpp`:11 | `Scrapeable_ErrorCategory::Describe` | enum<->string map -> constexpr | moderate | high | Pure static function: a switch over the literal-type enum Scrapeable_ErrorCodes returning compile-time string_view literals. No I/O, allocation, virtual dispatch, or mutable state. | Compile-time evaluable error-code description mapping; constexpr-usable. Needs the switch body relocated into the header for constexpr visibility. |
| 6 | `src/jandy/errors/string_conversion_errors.cpp`:11 | `StringConversion_ErrorCategory::Describe` | enum<->string map -> constexpr | moderate | high | Pure static function: a switch over the literal-type enum StringConversion_ErrorCodes returning compile-time string_view literals. No I/O, allocation, virtual dispatch, or side effects. | Compile-time evaluable enum->description lookup, foldable literals. Requires moving the switch body into the header for constexpr visibility. |
| 7 | `src/core/auth/entitlement.cpp`:94 | `Entitlement::Matches` | member-function -> constexpr | moderate | medium | Pure: compares m_Action/m_Selector (std::string members) against string_view args, no side effects, no I/O, no virtual dispatch. std::string comparisons are constexpr in C++23, so the whole body qualifies once the class is a literal type. | Compile-time authorization-match evaluation and inlining; would require Entitlement to have a constexpr ctor (also worthwhile) to be fully usable. |
| 8 | `src/core/kernel/temperature.h`:20 | `Temperature::Temperature(celsius quantity)` | constructor -> constexpr | moderate | medium | Pure member-init of m_TempInKelvin from a boost::units::quantity (a literal type); no I/O, allocation, virtual dispatch, or side effects. | Enables compile-time construction of Temperature constants and lets the whole value class be usable in constexpr contexts. |
| 9 | `src/core/kernel/temperature.h`:21 | `Temperature::Temperature(fahrenheit quantity)` | constructor -> constexpr | moderate | medium | Same as the celsius ctor: pure literal-type member init, no side effects; boost::units quantity conversion to kelvin is value-only. | Compile-time Temperature construction from a fahrenheit reading. |
| 10 | `src/core/kernel/temperature.h`:32 | `Temperature::InCelsius` | member-function -> constexpr | moderate | medium | Pure const accessor that only performs a boost::units unit conversion on the stored literal-type kelvin quantity; no I/O or mutable state. | Allows compile-time readout in celsius and constexpr evaluation of comparisons/derived constants. |
| 11 | `src/core/kernel/temperature.h`:33 | `Temperature::InFahrenheit` | member-function -> constexpr | moderate | medium | Pure const accessor performing only a boost::units conversion on the stored kelvin quantity; no side effects. | Compile-time fahrenheit readout. |
| 12 | `src/core/kernel/temperature.h`:36 | `Temperature::ConvertToTemperatureInCelsius` | member-function -> constexpr | moderate | medium | Static factory: multiplies a double by a unit_type and constructs a Temperature; pure, argument-only, no side effects or allocation. | Compile-time construction of Temperature from a raw celsius double (e.g. named constants / lookup tables of setpoints). |
| 13 | `src/core/kernel/temperature.h`:37 | `Temperature::ConvertToTemperatureInFahrenheit` | member-function -> constexpr | moderate | medium | Static factory identical in shape to the celsius one; pure arithmetic on the double argument plus a unit_type, no side effects. | Compile-time construction of Temperature from a raw fahrenheit double. |
| 14 | `src/core/serial/rfc2217/rfc2217_protocol_handler.cpp`:195 | `ProtocolHandler::SendCommand (IAC-escaping packet build)` | checksum / bit / framing math -> constexpr | moderate | medium | The packet-framing/IAC-escaping loop (lines 207-227) is a pure transform: given (command, data span) it deterministically builds the RFC2217 subnegotiation frame IAC\|SB\|COM_PORT_OPTION\|command\|<escaped data>\|IAC\|SE, doubling any 0xFF (IAC) byte. It touches only literal-type inputs and constexpr Constants, no socket/logging/state. Only the surrounding boost::asio::write is I/O. Extracting the loop into a free `constexpr std::vector<uint8_t> BuildCommandFrame(uint8_t, std::span<const uint8_t>)` (C++23 constexpr std::vector) makes the framing pure and compile-time evaluable; SendCommand then just writes the result. | Framing/DLE-style escaping becomes a pure, unit-testable (static_assert-able) function decoupled from socket I/O; enables compile-time validation of the wire frame and removes duplicated escaping logic risk. |
| 15 | `src/core/utility/case_insensitive_comparision.cpp`:8 | `CaseInsensitiveComparison` | free-function -> constexpr | moderate | medium | Pure single-character compare with no side effects, but currently out-of-line and calls std::tolower which is not constexpr. Replacing with a constexpr ASCII fold and moving the definition inline would make it (and EqualsCaseInsensitive) constexpr. | Enables constexpr case-insensitive comparisons throughout the device state machines; needs a constexpr ASCII to-lower helper. |
| 16 | `src/core/utility/case_insensitive_comparision.h`:16 | `EqualsCaseInsensitive` | free-function -> constexpr | moderate | medium | Pure comparison of two string_views via std::ranges::equal with a per-char lower-case predicate; no allocation/I/O. Blocked only because the underlying single-char compare uses std::tolower (<cctype>), which is not constexpr. | Compile-time case-insensitive equality for literal enum-token comparisons; requires a constexpr ASCII to-lower to replace std::tolower. |
| 17 | `src/jandy/devices/jandy_device_types.h`:49 | `m_KnownDeviceIdsList (device-class -> raw-id table) + ctor lookup + InstanceAddressesForClass` | lookup-table / array -> constexpr | moderate | medium | [DOWNGRADED] The non-literal-table diagnosis is correct, but the refactor is harder and the named-function benefit is overstated. The rows are ragged (SerialAdapter=2 ids, Jandy_Light=5, Unknown=1, rest=4), so a uniform std::array<std::array<>> does not fit; a flattened/span-with-count or std::array of small fixed structs is required. Critically, InstanceAddressesForClass returns std::vector<std::uint8_t> and push_backs into it (lines 66-82) — that heap-allocating return type cannot be constexpr-evaluated to a caller without changing the signature (e.g. to a fixed-size/span result), rippling to callers. Only the ctor's linear lookup (lines 18-28) can straightforwardly become constexpr over a literal table; the static-init-heap-removal benefit is genuine. | Replacing the std::list/std::vector table with a constexpr std::array of fixed-size arrays removes per-process static-init heap allocation and runtime list/vector construction, and would let the id->class lookup ctor and InstanceAddressesForClass become constexpr/ROM-able. |
| 18 | `src/jandy/options/options_jandy.cpp`:32 | `DefaultDeviceId` | free-function -> constexpr | moderate | medium | Pure function of a single enum argument: a plain switch returning a JandyDeviceId literal-type wrapper (uint8_t). No I/O, no allocation, no logging, no throwing, no runtime/device/config state. Result depends only on the argument. | Compile-time evaluable device-id defaults; the whole enum->id mapping folds to a constant. Enables constexpr use in default-table construction and const-folds the switch. |
| 19 | `src/jandy/startup/jandy_revision_capabilities.cpp`:43 | `DeriveRevisionCapabilities` | free-function -> constexpr | moderate | medium | Pure: result depends only on the revision string argument; RevisionCapabilities is a literal aggregate of char/bool; the inner at_least lambda is pure char arithmetic. No I/O, allocation-that-escapes, virtual, logging, or static state. | Compile-time derivation of the revision capability table for known-literal revision strings and unit-test constants; the revision->feature mapping becomes constant-foldable. Requires ParseRevisionLetter to be constexpr first. |
| 20 | `src/jandy/startup/jandy_revision_capabilities.cpp`:9 | `ParseRevisionLetter` | free-function -> constexpr | moderate | medium | Pure string scan returning std::optional<char> (literal type); in C++23 std::string and its ops are constexpr. Only blocker is <cctype> std::toupper/std::isspace/std::isalpha, which are not constexpr and locale-dependent. | Enables constexpr for ParseRevisionLetter and its caller DeriveRevisionCapabilities; also removes locale dependency (the ctype calls) by switching to explicit ASCII char arithmetic, making revision parsing locale-independent and compile-time evaluable. |
| 21 | `src/jandy/utility/spa_switch_assignment.cpp`:92 | `KnownSpaSwitchFunctions` | lookup-table / array -> constexpr | moderate | medium | The returned list is a fixed, controller-defined capability table of string literals with no runtime input; it is currently a function-local std::vector<std::string> built once via static initialisation and never mutated. | Replaces heap-allocating runtime static-init (14 std::string allocations) with a constexpr std::array<std::string_view, N> baked into ROM; also removes static-init/thread-safe-guard overhead. Return type would change from const std::vector<std::string>& to std::span<const std::string_view> (callers currently rely on vector<string>, so signature ripple is the cost). |
| 22 | `src/jandy/startup/jandy_startup_planner.cpp`:10 | `StartupPlanner::AQUALINKTOUCH_IDS / ONETOUCH_IDS / PDA_IDS / SERIALADAPTER_IDS` | lookup-table / array -> constexpr | significant | medium | Namespace-scope candidate-id tables initialised entirely from integer literals; the contents never change at runtime. Ideal as constexpr std::array<std::uint8_t,N> instead of runtime-initialised std::vector, avoiding heap allocation and static-init cost. | Removes four heap allocations + static init; puts the address tables in ROM/const data and makes them usable in constant expressions. Requires changing PlannedDevice::candidate_ids and SelectFreeAddress to accept a span/array rather than std::vector. |
| 23 | `src/core/http/server/routing/segment_template.h`:52 | `segment_template::has_modifier` | member-function -> constexpr | minor | low | Reads only literal-type members and calls the (constexpr-able) is_literal(); pure, no side effects. | Small: lets the modifier predicates participate in constant evaluation once the class ctor/members are constexpr. |
| 24 | `src/core/http/server/routing/segment_template.h`:31 | `segment_template::segment_template` | constructor -> constexpr | minor | low | Defaulted ctor of a literal-value class (std::string is a literal type in C++23; bool and enum members); no runtime-only initialisation. | Allows constexpr construction, a prerequisite for the accessor/comparator functions above being usable in constant expressions. |
| 25 | `src/core/kernel/pool_configurations.cpp`:6 | `ToDisplayString(PoolConfigurations)` | enum<->string map -> constexpr | minor | low | [DOWNGRADED] Body is a pure enum switch returning string literals, so `constexpr` compiles — but the claimed benefit is essentially nil and the framing is misleading: a constexpr std::string allocation cannot escape a constant evaluation (transient-allocation rule), and every real caller consumes the result at runtime. So it never actually evaluates at compile time. Correct refactor for any payoff is a std::string_view table, not tagging this signature constexpr. Real-but-marginal, matching the 'low' confidence. | Permits compile-time label lookup; low real payoff since callers use it at runtime and the return escapes as a heap std::string. |
| 26 | `src/core/kernel/system_boards.cpp`:6 | `ToDisplayString(SystemBoards)` | enum<->string map -> constexpr | minor | low | [DOWNGRADED] Same as PoolConfigurations: pure enum-to-literal switch so `constexpr` is syntactically legal, but the returned std::string is a transient constexpr allocation that cannot persist to runtime and all callers use it at runtime, so no compile-time evaluation ever occurs. The candidate's own note (string_view table is the real form) is the accurate assessment; benefit is marginal, not 'minor refactor for constexpr label lookup'. | Compile-time label lookup possible; marginal here as the result is a runtime-consumed heap string. A constexpr std::string_view table would be the higher-value form. |
| 27 | `src/jandy/devices/capabilities/emulated.cpp`:14 | `Emulated::IsEmulated / IsEmulationSuppressed / IsEmulationActive` | member-function -> constexpr | minor | low | [DOWNGRADED] IsEmulated() is a pure-virtual override of IEmulatedDevice::IsEmulated (iemulateddevice.h:14) — virtual dispatch is explicitly excluded by the rules, and to be constexpr it must also be moved from emulated.cpp into the header (all three are currently out-of-line definitions, so refactor is more than 'add keyword'). The state (m_IsEmulated, m_EmulationSuppressed) is always runtime-constructed, so no real call site can constant-evaluate these; benefit reduces to enabling inlining of the non-virtual pair. Real but harder and smaller than a straightforward constexpr win, matching the candidate's own low confidence. | constexpr predicates if the interface allows; marginal since values come from runtime construction, so mainly enables inlining. |
| 28 | `src/jandy/messages/jandy_message_text_helpers.h`:76 | `AqualinkAutomate::Messages::Text::ExtractTrailingAsciiPayload` | free-function -> constexpr | minor | low | Pure: reads a std::span<const uint8_t> plus the constexpr JandyMessage::PACKET_FOOTER_LENGTH, does bounds arithmetic and builds a std::string; no I/O, allocation escaping, virtual dispatch, logging or static state. In C++23 constexpr std::string is available so the whole body is constant-evaluable. | Enables compile-time evaluation for any fixed-input caller and lets the compiler constant-fold the footer/underflow guard; currently no constant-eval caller exists so the practical gain is small. |
| 29 | `src/jandy/messages/jandy_message_text_helpers.h`:114 | `AqualinkAutomate::Messages::Text::ExtractTrailingDisplayLine` | free-function -> constexpr | minor | low | Pure: only reads the span and constexpr footer length, strips trailing pad bytes and maps NUL->space via the already-constexpr SanitisePrintableAsciiByte; no logging, I/O, allocation-that-escapes, or mutable static state. constexpr std::string in C++23 makes the body constant-evaluable. | Allows compile-time display-line extraction and constant-folding of the pad-stripping loop for literal inputs; limited practical benefit today as callers pass runtime wire spans. |
| 30 | `src/jandy/messages/jandy_message_text_helpers.h`:57 | `AqualinkAutomate::Messages::Text::AppendSanitisedAscii` | free-function -> constexpr | minor | low | Pure transform: appends each byte through the constexpr SanitisePrintableAsciiByte into the passed-in std::string; touches no I/O, no static state, no virtual dispatch. std::string ops are constexpr in C++23. | Makes the sanitised-append usable in constant expressions and lets it constant-fold when both inputs are literal; low practical value as it is a small helper over runtime data. |
| 31 | `src/jandy/messages/light/light_message_status.cpp`:25 | `LightMessage_Status::IsOn` | member-function -> constexpr | minor | low | Pure predicate: compares the literal-type enum member m_State against LightStates::On with no side effects, I/O, or virtual dispatch. | Marks intent as a pure query and permits compile-time evaluation; marginal since it is a trivial one-liner and requires making the accessor header-visible. |
| 32 | `src/jandy/navigation/onetouch_menu_model.cpp`:663 | `OneTouchPageCapabilityRequirement` | enum<->string map -> constexpr | minor | low | [DOWNGRADED] Body at onetouch_menu_model.cpp:663-684 is indeed a pure PageId switch returning fixed strings. But a std::string built during constexpr evaluation cannot persist to runtime (non-transient constexpr allocation is ill-formed), so as written the function is not usable in any constant expression that keeps its result — the constexpr marker would be effectively decorative. The real refactor to std::optional<std::string_view> over static literals is a signature/type change touching every caller, not the 'minor' add-constexpr edit. The candidate's own text already concedes this, so DOWNGRADED confirms: real but low value and larger-than-minor to make genuinely useful. | Compile-time capability classification is possible, but the returned std::string heap-allocates and escapes, so real compile-time use is limited; the honest win is marking the function pure/constexpr-eligible. Consider returning std::optional<std::string_view> over static literals to make it genuinely useful at compile time. |
| 33 | `src/core/utility/slugify.h`:13 | `Slugify` | free-function -> constexpr | significant | low | [DOWNGRADED] Blockers (std::isalnum/std::tolower non-constexpr, lines 20/22) are real and correctly identified, but the claimed benefit overstates it: even with constexpr std::string (C++23), a constexpr std::string cannot escape to a runtime static, and every call site passes a runtime std::string& (device labels), so no call site would actually evaluate at compile time. The refactor yields a constexpr-usable function with essentially no real-world constant-folding — self-rated low confidence is warranted, benefit smaller than described. | Compile-time slug generation for literal labels; requires replacing the locale-dependent ctype calls with constexpr ASCII predicates. |

## Rejected false positives (3)

| File | Symbol | Why rejected |
|---|---|---|
| `src/core/kernel/ph.cpp` | `pH::ClampAndRound` | The 'why' rests on a false premise: std::roundf is NOT constexpr in C++23. P0533 made std::fabs/std::abs and simple manipulation functions constexpr, but the rounding family (round/roundf/ceil/floor/trunc) was made constexpr by P1383, which lands in C++26 — not C++23. Line 37 calls std::roundf, so the body cannot be a constant expression under C++23, blocking the whole function. |
| `src/core/kernel/ph.cpp` | `pH::pH(boost::float32_t)` | The ctor's member-init calls ClampAndRound, which is not a constant expression in C++23 because it uses std::roundf (constexpr only from C++26/P1383). A constexpr constructor whose body can never be constant-evaluated is ill-formed (no diagnostic required); the constexpr claim is only valid once roundf-dependence is removed. |
| `src/core/kernel/ph.cpp` | `pH::operator=(boost::float32_t)` | Line 22 assigns ClampAndRound(value), which is not a constant expression in C++23 (std::roundf non-constexpr until C++26). The constexpr-since-C++14 mutation rule is irrelevant while the RHS call cannot be constant-evaluated. |
