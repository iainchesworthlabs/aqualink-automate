name: CI

on:
  # Only the long-lived branches build on push; feature branches build via their
  # pull_request (a WIP push to a branch without a PR runs nothing, and a push to
  # a branch WITH a PR is covered by the PR run). This removes the per-WIP-push
  # full-matrix runs and the push+PR double-run on every feature branch.
  push:
    branches: ["main", "develop"]
  pull_request:
    branches: ["develop", "main"]

# Group by the source branch across BOTH event types (head_ref is set for PRs,
# ref_name for pushes), so a branch can only have one CI run in flight. This
# collapses the push run and the pull_request run of the same commit into one
# lane — notably on a develop->main promotion, where the develop push and the
# PR both target commit on `develop` and would otherwise run two Windows MSVC
# builds at once on the single self-hosted runner and collide.
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # Classify the change so docs-only work can skip the build/test matrix. The
  # binary is unaffected by Markdown, the docs/ tree, the MkDocs config, or the
  # docs publish workflow, so the heavy jobs below are gated on `code == 'true'`.
  # The "CI Status" aggregator still runs and reports, so a required check is
  # never left pending. Classification uses the GitHub API (no checkout / no
  # fetch-depth juggling) and is conservative: anything not clearly docs is code.
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      code: ${{ steps.detect.outputs.code }}
    steps:
      - name: Classify changed files
        id: detect
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          # Paths that NEVER affect the build/test matrix.
          docs_re='^(docs/|mkdocs\.yml$|\.github/workflows/docs\.yml$|LICENSE|.*\.md$)'

          if [ "${{ github.event_name }}" = "pull_request" ]; then
            files=$(gh api --paginate "repos/$REPO/pulls/${{ github.event.pull_request.number }}/files" --jq '.[].filename')
          else
            before='${{ github.event.before }}'
            if [ -z "$before" ] || [ "$before" = "0000000000000000000000000000000000000000" ]; then
              files=$(gh api "repos/$REPO/commits/${{ github.sha }}" --jq '.files[].filename')
            else
              files=$(gh api "repos/$REPO/compare/$before...${{ github.sha }}" --jq '.files[].filename')
            fi
          fi

          echo "Changed files:"; printf '%s\n' "$files" | sed 's/^/  /'

          code=false
          while IFS= read -r f; do
            if [ -z "$f" ]; then continue; fi
            if ! printf '%s\n' "$f" | grep -qE "$docs_re"; then code=true; break; fi
          done <<< "$files"
          # No detectable files (defensive) -> run the full matrix.
          if [ -z "$files" ]; then code=true; fi

          echo "code=$code" >> "$GITHUB_OUTPUT"
          echo "Build/test matrix needed: $code"

  # Enforce the branch naming convention on every PR. Branches must be
  # <type>/<name> with an allowed Conventional Commit type (see CONTRIBUTING.md).
  # develop/main are accepted as PR heads so the develop->main release-promotion
  # PR (head_ref == develop) is never blocked. This "Branch Name" check is a
  # required status check on develop/main, so a misnamed branch cannot merge. (A
  # server-side branch-name ruleset would also block at push/creation time, but
  # that rule type needs a GitHub organization plan — unavailable on a user repo.)
  branch-name:
    name: Branch Name
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Validate head branch name
        shell: bash
        env:
          HEAD_REF: ${{ github.head_ref }}
        run: |
          if [[ "$HEAD_REF" == "develop" || "$HEAD_REF" == "main" ]]; then
            echo "Core branch '$HEAD_REF' — allowed (release promotion)."
            exit 0
          fi
          pattern='^(feat|fix|docs|ci|test|refactor|chore|build|perf)/[a-z0-9._-]+$'
          if [[ "$HEAD_REF" =~ $pattern ]]; then
            echo "Branch '$HEAD_REF' conforms to <type>/<name>."
          else
            echo "::error title=Invalid branch name::'$HEAD_REF' must be <type>/<name> where <type> is one of: feat, fix, docs, ci, test, refactor, chore, build, perf (for example feat/spaside-led-map). See CONTRIBUTING.md."
            exit 1
          fi

  # Guard the web UI translation catalogs: every key referenced in code must
  # exist in en.js, and every shipped locale must have full key + placeholder
  # parity with English. Runtime-level guards (missing-key console warnings,
  # pseudo-locale hardcoded-string scan) live in e2e/i18n.spec.ts; see
  # docs/i18n.md.
  i18n-catalogs:
    name: I18n Catalogs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Check catalog completeness and locale parity
        shell: pwsh
        run: ./scripts/check-i18n-keys.ps1 -Root $PWD

  # Enforce the platform-isolation principle (docs/platform-isolation.md): no
  # OPERATING-SYSTEM preprocessor macro (_WIN32, __APPLE__, __linux__, …) may gate
  # any shared, non-platform source file. OS-divergent code belongs in
  # src/core/platform/<os>/ selected by the if(WIN32)/if(LINUX)/if(APPLE) CMake
  # blocks. Compiler/arch/build-feature macros are a separate concern and are not
  # flagged. A source-only check, so it runs cheaply on every PR (no build needed).
  platform-macros:
    name: Platform Macros
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Check for OS macros in shared code
        shell: pwsh
        run: ./scripts/check-os-macros.ps1 -Root $PWD

  # The configure/build/test matrix lives in the reusable _build.yml so CI and the
  # release pipeline share one definition and can never drift. Runner selection
  # (vars.RUNNER_LINUX / RUNNER_WINDOWS) is handled inside that workflow.
  # upload_installtree publishes the Linux install tree that e2e-ui consumes below
  # (so e2e no longer recompiles the app).
  build-and-test:
    name: Build & Test
    needs: changes
    if: needs.changes.outputs.code == 'true'
    uses: ./.github/workflows/_build.yml
    with:
      upload_installtree: true

  # Drive the real UI against a replay fixture in a browser. Consumes the binary
  # built by build-and-test (the installtree-linux-gcc artifact) instead of
  # recompiling the app. Trade-off: gated behind the whole build matrix, so it
  # starts later than when it built its own binary — but it never recompiles.
  e2e-ui:
    name: E2E UI (Playwright)
    needs: [changes, build-and-test]
    # Skip for docs-only changes; otherwise keep the fork guard: fork PRs must not
    # run on the persistent self-hosted runners (untrusted code on a long-lived
    # host). Same-repo PRs and push/release events are unaffected.
    if: >-
      needs.changes.outputs.code == 'true' &&
      (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    runs-on: ${{ fromJSON(vars.RUNNER_LINUX || '["ubuntu-latest"]') }}

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          submodules: recursive

      - name: Download Linux install tree
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: installtree-linux-gcc
          path: installtree-art

      - name: Unpack install tree
        shell: bash
        run: |
          mkdir -p installtree
          tar xzf installtree-art/installtree-linux-gcc.tar.gz -C installtree
          chmod +x installtree/bin/aqualink-automate

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install Playwright + Chromium
        shell: bash
        run: |
          npm ci
          # `--with-deps` runs Playwright's apt helper, which errors on the
          # self-hosted runner's Ubuntu 26.04 ("does not support chromium on
          # ubuntu26.04-x64"). That runner already carries the browser system
          # libs from prior runs, so fetching just the browser binary is enough;
          # fall back to it when the deps path is unsupported (keeps a stock
          # GitHub-hosted ubuntu-latest runner working too).
          npx playwright install --with-deps chromium || npx playwright install chromium

      # The four runs mirror the modes encoded in playwright.config.ts. Each boots
      # the downloaded binary against the replay fixture; env selects the mode.
      #   * default        -> every spec except auth (history/scheduler "disabled" paths)
      #   * AUTH_TOKEN      -> only auth.spec.ts      (token-gated WS auth)
      #   * HISTORY_DB      -> only trends.spec.ts    (recorded series + chart)
      #   * SCHEDULES_FILE  -> only schedules.spec.ts (schedule CRUD)
      - name: E2E — default (unauthenticated)
        shell: bash
        run: npx playwright test
        env:
          AQUALINK_EXE: ${{ github.workspace }}/installtree/bin/aqualink-automate

      # Identity system (AQUALINK_AUTH_MODE=enabled) — one spec per step. The three
      # identity specs have incompatible auth-store preconditions (auth.spec needs an
      # EMPTY store for the first-run wizard; admin.spec self-seeds an admin; guest.spec
      # configures the Guest scope), so each runs in its own invocation against a fresh
      # --auth-state-dir (see playwright.config.ts).
      - name: E2E — identity (first-run + sessions)
        shell: bash
        run: npx playwright test e2e/auth.spec.ts
        env:
          AQUALINK_EXE: ${{ github.workspace }}/installtree/bin/aqualink-automate
          AQUALINK_AUTH_MODE: enabled

      - name: E2E — identity (admin management)
        shell: bash
        run: npx playwright test e2e/admin.spec.ts
        env:
          AQUALINK_EXE: ${{ github.workspace }}/installtree/bin/aqualink-automate
          AQUALINK_AUTH_MODE: enabled

      - name: E2E — identity (guest mode + kiosk PIN)
        shell: bash
        run: npx playwright test e2e/guest.spec.ts
        env:
          AQUALINK_EXE: ${{ github.workspace }}/installtree/bin/aqualink-automate
          AQUALINK_AUTH_MODE: enabled

      - name: E2E — history enabled
        shell: bash
        run: npx playwright test
        env:
          AQUALINK_EXE: ${{ github.workspace }}/installtree/bin/aqualink-automate
          AQUALINK_HISTORY_DB: ${{ runner.temp }}/e2e-history.db

      - name: E2E — scheduler enabled
        shell: bash
        run: npx playwright test
        env:
          AQUALINK_EXE: ${{ github.workspace }}/installtree/bin/aqualink-automate
          AQUALINK_SCHEDULES_FILE: ${{ runner.temp }}/e2e-schedules.json

      - name: Upload Playwright report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

  matter-bridge:
    name: Matter Bridge (Node)
    needs: changes
    # Skip for docs-only changes; otherwise keep the fork guard (untrusted code
    # must not run on the persistent self-hosted runners).
    if: >-
      needs.changes.outputs.code == 'true' &&
      (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    runs-on: ${{ fromJSON(vars.RUNNER_LINUX || '["ubuntu-latest"]') }}
    defaults:
      run:
        working-directory: matter-bridge

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: matter-bridge/package-lock.json

      - name: Install
        run: npm ci

      - name: Typecheck (incl. matter.js bridge)
        run: npm run typecheck

      - name: Build
        run: npm run build

      - name: Unit tests
        run: npm test

  version-check:
    name: Version Check
    needs: changes
    if: needs.changes.outputs.code == 'true' && github.event_name == 'pull_request' && github.base_ref == 'main'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0

      - name: Check version bump
        shell: bash
        run: |
          # The project version is derived from git tags (see GitVersionDerivation.cmake),
          # not from a literal in CMakeLists.txt, so compare what `git describe` resolves on
          # the PR head versus the base branch.
          HEAD_DESCRIBE=$(git describe --tags --match "v*" --always HEAD 2>/dev/null || echo "none")
          BASE_DESCRIBE=$(git describe --tags --match "v*" --always "origin/${{ github.base_ref }}" 2>/dev/null || echo "none")

          echo "Base (${{ github.base_ref }}): $BASE_DESCRIBE"
          echo "Head: $HEAD_DESCRIBE"

          # A PR into main is a release promotion. Block (don't just warn) when the
          # resolved version is identical to the base — i.e. there is nothing new to
          # release. Distinct `git describe` output (a different commit distance or a
          # new tag) is enough to pass; this only fails a genuinely empty promotion.
          if [[ "$HEAD_DESCRIBE" == "$BASE_DESCRIBE" ]]; then
            echo "::error::Version is unchanged between ${{ github.base_ref }} ($BASE_DESCRIBE) and this PR head ($HEAD_DESCRIBE). Nothing new to release."
            exit 1
          fi
          echo "Version advances: base=$BASE_DESCRIBE -> head=$HEAD_DESCRIBE"

  # Cold, from-source Docker build kept deliberately as the gate that the
  # multi-stage Dockerfile + vcpkg toolchain still build end to end (the assembled
  # runtime path in release.yml's docker-publish skips this, so this is its safety
  # net). Catches a vcpkg/Dockerfile/ABI regression that the assembly path can't.
  docker-verify:
    name: Docker Verification
    needs: changes
    # Skip for docs-only changes; otherwise keep the fork guard (untrusted code
    # must not run on the persistent self-hosted runners).
    if: >-
      needs.changes.outputs.code == 'true' &&
      (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    runs-on: ${{ fromJSON(vars.RUNNER_LINUX || '["ubuntu-latest"]') }}
    # Surfaced so the "Login to Docker Hub" step's `if:` can gate on the optional
    # secret being present. See release.yml / docs/releasing.md.
    env:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          submodules: recursive

      - uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4

      # Authenticate to Docker Hub when credentials are configured so base-image
      # pulls (ubuntu:26.04, node:24-bookworm-slim) use the higher authenticated
      # rate limit instead of the anonymous per-IP quota. Gated so fork PRs (which
      # receive no secrets) still build anonymously.
      - name: Login to Docker Hub
        if: env.DOCKERHUB_USERNAME != ''
        uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0 # v4
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Both builds are wrapped in a bounded retry so a transient Docker Hub CDN
      # blob timeout pulling a base image doesn't fail CI. A retry reuses the
      # BuildKit layer cache + the in-Dockerfile ccache/vcpkg cache mounts, so it
      # only re-fetches the layer that timed out. docker/setup-buildx-action makes
      # its builder the default, so `docker buildx build` here is equivalent to the
      # docker/build-push-action invocations it replaces.
      - name: Build CI target
        uses: nick-fields/retry@ad984534de44a9489a53aefd81eb77f87c70dc60 # v4
        with:
          shell: bash
          timeout_minutes: 90
          max_attempts: 3
          retry_wait_seconds: 30
          retry_on: error
          command: docker buildx build --target ci .

      - name: Build runtime target
        uses: nick-fields/retry@ad984534de44a9489a53aefd81eb77f87c70dc60 # v4
        with:
          shell: bash
          timeout_minutes: 90
          max_attempts: 3
          retry_wait_seconds: 30
          retry_on: error
          command: docker buildx build --target runtime --load --tag aqualink-automate:verify .

      # The runtime image now bundles the Matter sidecar (built by the matter-builder
      # stage). The --version smoke test exercises the app via the supervising
      # entrypoint; MATTER_ENABLED=false keeps it focused on the C++ binary.
      - name: Verify runtime starts (app)
        run: docker run --rm -e MATTER_ENABLED=false aqualink-automate:verify --version

      # Confirm the bundled sidecar is present and its Node runtime works (without
      # actually booting the bridge, which would block).
      - name: Verify Matter sidecar is bundled
        run: docker run --rm --entrypoint sh aqualink-automate:verify -c "node --version && test -f /opt/matter-bridge/dist/index.js && echo sidecar-present"

  # Single aggregated gate. This is the required status check on develop/main
  # (alongside "Branch Name") INSTEAD of the individual matrix leaves, because
  # those leaves come from the reusable _build.yml and never report when the
  # caller is skipped — which would leave a required check pending forever on a
  # docs-only change. This job always runs and passes as long as no required job
  # FAILED; jobs that were skipped (docs-only changes, fork-gated jobs) count as
  # OK. version-check is intentionally excluded so it stays advisory, as today.
  ci-status:
    name: CI Status
    if: always()
    needs: [changes, build-and-test, e2e-ui, matter-bridge, docker-verify, i18n-catalogs, platform-macros]
    runs-on: ubuntu-latest
    steps:
      - name: Gate on required job results
        shell: bash
        env:
          R_CHANGES: ${{ needs.changes.result }}
          R_BUILD: ${{ needs.build-and-test.result }}
          R_E2E: ${{ needs.e2e-ui.result }}
          R_MATTER: ${{ needs.matter-bridge.result }}
          R_DOCKER: ${{ needs.docker-verify.result }}
          R_I18N: ${{ needs.i18n-catalogs.result }}
          R_OSMACROS: ${{ needs.platform-macros.result }}
        run: |
          set -euo pipefail
          fail=0
          for pair in "changes:$R_CHANGES" "build-and-test:$R_BUILD" "e2e-ui:$R_E2E" "matter-bridge:$R_MATTER" "docker-verify:$R_DOCKER" "i18n-catalogs:$R_I18N" "platform-macros:$R_OSMACROS"; do
            job="${pair%%:*}"; result="${pair#*:}"
            echo "  $job: $result"
            if [ "$result" = "failure" ] || [ "$result" = "cancelled" ]; then
              echo "::error::$job reported '$result'."
              fail=1
            fi
          done
          if [ "$fail" -ne 0 ]; then
            echo "CI failed — see the failing job(s) above."
            exit 1
          fi
          echo "All required CI jobs passed or were skipped."
