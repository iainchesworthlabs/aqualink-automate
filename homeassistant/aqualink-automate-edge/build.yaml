# GENERATED from ../aqualink-automate by scripts/gen-homeassistant-edge-addon.ps1 — DO NOT EDIT.
# Change the stable add-on, then re-run the generator and commit (CI enforces no drift).
# Local-build base images: the Supervisor builds the add-on image ON THE USER'S
# DEVICE by layering bashio + run.sh (see Dockerfile) on top of the published
# multi-arch app image. Docker resolves the correct arch layer from the manifest,
# so both entries point at the same tag.
#
# This tag MUST match config.yaml `version` (the app release version). Bump both
# together on every release; the CI lint/version check enforces it.
build_from:
  aarch64: "ghcr.io/iainchesworth/aqualink-automate:0.12.0-beta.4"
  amd64: "ghcr.io/iainchesworth/aqualink-automate:0.12.0-beta.4"
labels:
  org.opencontainers.image.title: "Aqualink Automate"
  org.opencontainers.image.source: "https://github.com/iainchesworth/aqualink-automate"
